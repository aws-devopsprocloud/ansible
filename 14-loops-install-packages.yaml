- name: Installing packages 
  hosts: localhost 
  become: yes
  tasks: 
  - name: Installing multiple packages
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - git 
    - nginx 
    - postfix 